import * as types from '../../actions/types';

const initialState = {

}

export default function loginReducers(state = initialState, action) {
  switch(action.type) {
    default:
      return state;
  }
}
